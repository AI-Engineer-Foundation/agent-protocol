import { Clients } from '@/components/Clients'

export const metadata = {
  title: 'Agent Protocol Client',
  description:
    'The Agent Protocol Clients are libraries that simplifies interaction Agent Protocol.',
}

export const sections = []

# Agent Protocol Client libraries

Client libraries are used to interact with the Agents. You can use them to communicate with agents from your application (e.g. web app). {{ className:"lead" }}

<Clients />
